#include<stdio.h>
#include <sys/types.h>
#include <unistd.h>

/*
Programa que calcula la distancia para la que se debe realizar el salto.
*/

int main(int argc, char* argv[]){
	int x = 0;
	a:  
	x++; //Instrucción a saltar
	b:   
	printf("Diferencia: %d\n", &&b - &&a);
}
